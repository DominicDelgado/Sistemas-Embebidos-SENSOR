<?xml version="1.0" encoding="utf-8"?>
<!--
    AndroidManifest.xml — Tracker Alertas

    Declara permisos, actividades y servicios de la aplicación.

    Permisos importantes:
      - POST_NOTIFICATIONS: necesario desde Android 13 para mostrar notificaciones
      - USE_FULL_SCREEN_INTENT: necesario para alertas a pantalla completa
      - SYSTEM_ALERT_WINDOW: para mostrar la Activity sobre otras apps
      - WAKE_LOCK + RECEIVE_BOOT_COMPLETED: para despertar el dispositivo
      - SCHEDULE_EXACT_ALARM + FOREGROUND_SERVICE: requeridos por FCM en Android moderno
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permisos -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TrackerAlertas"
        tools:targetApi="31">

        <!-- Pantalla principal (registro de token, selector de audio, botón test) -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            Pantalla de ALERTA: aparece sobre la pantalla bloqueada estilo llamada.
            Atributos clave:
              - showWhenLocked + showOnLockScreen: visible sobre el bloqueo
              - turnScreenOn: enciende la pantalla automáticamente
              - excludeFromRecents: no aparece en la lista de apps recientes
              - launchMode singleTask: evita instancias múltiples si llegan varios push
        -->
        <activity
            android:name=".AlertaActivity"
            android:exported="false"
            android:showOnLockScreen="true"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:excludeFromRecents="true"
            android:launchMode="singleTask"
            android:theme="@style/Theme.TrackerAlertas" />

        <!-- Servicio de Firebase Cloud Messaging -->
        <service
            android:name=".AlertaFirebaseService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- Canal de notificación por defecto que usa FCM -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="canal_alertas" />

    </application>

</manifest>